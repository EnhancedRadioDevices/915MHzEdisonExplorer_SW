
import spi_serial

# TODO: create commands to control the CC1110

if __name__ == "__main__":
    while True:
		try:
			cmd = raw_input()
			#TODO: handle command
			
			#if waiting for packet, get packet
		except:
			break
